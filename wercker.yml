box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: wintersmith build
        code: ./wintersmith build -v -o ./build
deploy:
  steps:
    - gh-pages:
        token: $GIT_TOKEN
        domain: fkrauthan.de
        basedir: build
