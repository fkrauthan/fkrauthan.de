box: node
build:
  steps:
    - npm-install
    - script:
        name: wintersmith build
        code: ./wintersmith build -v -o ./build
deploy:
  steps:
    - gh-pages:
        token: $GIT_TOKEN
        basedir: build
