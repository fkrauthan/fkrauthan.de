box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: wintersmith build
        code: ./wintersmith build -v -o ./build
deploy:
  steps:
    - lukevivier/gh-pages@0.2.0:
        token: $GIT_TOKEN
        basedir: build
