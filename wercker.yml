box: node
build:
  steps:
    - npm-install
    - script:
        name: wintersmith build
        code: ./wintersmith build -v -o ./build
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: www.fkrauthan.de
        basedir: build
